[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "{{ cookiecutter.project_slug }}"
version = "{{ cookiecutter.version }}"
description = "A professional Python project structure"
readme = "README.md"
requires-python = ">={{ cookiecutter.python_version }}"
authors = [
    {name = "Developer"}
]
dependencies = [
    "requests",
    "python-dotenv",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "black",
    "ruff",
    "mypy"
]

[project.scripts]
{{ cookiecutter.project_slug }} = "{{ cookiecutter.pkg_name }}.main:run_app"

[tool.ruff]
line-length = 88
select = ["E", "F", "I"]